<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
</head>
<body>
    <h1>Blog Yazıları</h1>
    <ul>
        <% for (let i = 0; i < posts.length; i++) { %>
            <li><%= posts[i].title %></li>
            <p><%= posts[i].content %></p>
        <% } %>
    </ul>
</body>
</html>
